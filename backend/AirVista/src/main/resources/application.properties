spring.application.name=AirVista
spring.datasource.url=jdbc:postgresql://localhost:5432/StudEx
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


#
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG


#
application.security.jwt.expiration=86400000

# Refresh Token expiration time in milliseconds (e.g., 7 days)
application.security.jwt.refresh-token.expiration=604800000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
logging.level.org.springframework.mail=DEBUG
logging.level.jakarta.mail=DEBUG
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test.connection=true
spring.mail.properties.mail.debug=true

#RazorPay
razorpay.key.id=${RAZORPAY_KEY_ID}
razorpay.key.secret=${RAZORPAY_KEY_SECRET}

# AviationStack API Configuration
aviationstack.api.base-url=http://api.aviationstack.com/v1
aviationstack.api.access-key=0016b71710e998659050fd6e33994a82
